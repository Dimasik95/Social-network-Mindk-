// import PropTypes from 'prop-types';
import AboutUsersPropTypes from './aboutUsersPropTypes';

const UsersPropTypes = {
	AboutUsersPropTypes,
	// friends: PropTypes.arrayOf(PropTypes.shape(UsersPropTypes)),
};

export default UsersPropTypes;